1.设置默认的chunk大小
2.设置默认的page大小
set时候自动创建 key + value = 100
如何分配块:
chunk(基础)*f^(n - 1) = 块大小
最大的n大小 page / chunk

(100 + item结构体大小) 

一共 64M 内存分配
每一个页 1M 内存
每一个块的基本大小80字节
增长因子1.25

计算分配的最多页数：那一共可以分配 64个页面 

计算分配的最大阶级数 1M / chunk(基础) = F的指数
再求出其F指数是1.25多少倍:42

初始化每个块随着因子的增长的大小
利用公式求出每个倍数对应的大小：chunk(基础)*f^(n - 1) = 块大小（字节对齐） 有序的

查找合适的大小：利用二分查找找到合适的大小

内存slab保存形式
hash表形式保存内存信息。
利用基础大小迅速定位到指定的slab，并找到空余的空间。

